<%= form_for(wagon) do |f| %>
    <%= render 'common/errors', resource: @wagon %>

    <div class="field">
      <%= f.label :train, 'Поезд' %>
      <%= f.collection_select :train_id, Train.all, :id, :number %>
    </div>

    <div class="field">
      <%= f.label :wagon_type, 'Тип' %>
      <%= f.select :wagon_type, ['Купейный','Плацкартный'] %>
    </div>

    <div class="field">
      <%= f.label :high, 'Количество верхних мест' %>
      <%= f.text_field :high %>
    </div>

    <div class="field">
      <%= f.label :low, 'Количество нижних мест' %>
      <%= f.text_field :low %>
    </div>


    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
